div
  p Punkte erreicht: #{n_params.points_achieved} von #{n_params.points_required}
div
  p Zeit: #{n_params.time_elapsed / 1000}s#{n_params.time_limit != 0 ? ", Limit war: " + (n_params.time_limit / 1000) + "s" : ""}
div
  p
    | Übung war 
    strong #{success ? 'erfolgreich' : 'nicht erfolgreich'}
div
  a(href="/logicjs/app/home")
    button zurück
